import styled from "styled-components";

const Header = styled.header`

`
const MenuButton = styled.button`
width: 20px;
`
const MenuButtonIcon = styled.img`
width: 20px;
`

const Styled = {
    Header, MenuButton, MenuButtonIcon
}

export default Styled;